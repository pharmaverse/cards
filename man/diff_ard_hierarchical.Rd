% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff_ard_hierarchical.R
\name{diff_ard_hierarchical}
\alias{diff_ard_hierarchical}
\title{Rate Differences for Stacked Hierarchical ARDs}
\usage{
diff_ard_hierarchical(x, levels = NULL)
}
\arguments{
\item{x}{(\code{card})\cr
a stacked hierarchical ARD of class \code{'card'} created using
\code{\link[=ard_stack_hierarchical]{ard_stack_hierarchical()}}. Must contain the \code{p} statistic and at least one
\code{by} variable.}

\item{levels}{(\code{list})\cr
a list specifying the two groups being differenced, \verb{p(group 1) - p(group 2)}.
Required unless \code{x} has a single \code{by} variable with exactly two levels, in
which case the difference defaults to the first level minus the second level
(in the order the levels appear in the source data). Two forms are accepted:
\itemize{
\item a \emph{flat named list} keyed by a single \code{by} variable whose name is repeated
to give its two levels in order, e.g.
\code{list(TRTA = "Xanomeline High Dose", TRTA = "Placebo")}. When \code{x} has more
than one \code{by} variable, every other \code{by} variable must also be named once
to pin it to a single level.
\item a list of \emph{two named lists}, each fully specifying one group's combination
of \code{by} variable levels, e.g.
\code{list(list(TRTA = "Xanomeline High Dose"), list(TRTA = "Placebo"))} or, with
two \code{by} variables,
\code{list(list(TRTA = "Xanomeline High Dose", SEX = "F"), list(TRTA = "Placebo", SEX = "F"))}.
This form is required when \code{x} has more than one \code{by} variable.
}

Defaults to \code{NULL}.}
}
\value{
an ARD data frame of class 'card'
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}\cr

Calculate the difference in event rates between two groups of a stacked
hierarchical ARD created with \code{\link[=ard_stack_hierarchical]{ard_stack_hierarchical()}}. For every node in
the hierarchy (e.g. each system organ class and each preferred term), the
rate (\code{p} statistic) of a second group is subtracted from the rate of a first
group and returned under a new statistic named \code{"estimate"}.

The input ARD must contain the \code{p} statistic and at least one \code{by} variable.
The \code{by} (grouping) dimension is collapsed in the result---only the \code{estimate}
rows are returned.
}
\details{
Rates are stored on the \verb{[0, 1]} scale (as \code{p} is), so the returned \code{estimate}
values lie in \verb{[-1, 1]}. A hierarchy node observed in only one of the two
groups is treated as a rate of \code{0} in the group where it is absent.
}
\examples{
\dontshow{if ((identical(Sys.getenv("NOT_CRAN"), "true") || identical(Sys.getenv("IN_PKGDOWN"), "true"))) withAutoprint(\{ # examplesIf}
ard <- ard_stack_hierarchical(
  ADAE,
  variables = c(AESOC, AEDECOD),
  by = TRTA,
  denominator = ADSL,
  id = USUBJID
)

# difference between two of the three treatment arms
diff_ard_hierarchical(ard, levels = list(TRTA = "Xanomeline High Dose", TRTA = "Placebo"))

# equivalent, using the two-cell form
diff_ard_hierarchical(
  ard,
  levels = list(list(TRTA = "Xanomeline High Dose"), list(TRTA = "Placebo"))
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=sort_ard_hierarchical]{sort_ard_hierarchical()}}, \code{\link[=filter_ard_hierarchical]{filter_ard_hierarchical()}}
}
